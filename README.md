# Mealibrio Webapp

Web application repository for Mealibrio.

Repository access and the local checkout have been verified. This repository is
included in `mealibrio-dockerizer` as the `mealibrio-webapp` submodule.

Application implementation and development instructions will follow.
